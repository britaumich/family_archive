<%= turbo_frame_tag 'new_tag' do %>  
  <%= form_with(model: tag, data: { controller: "autosubmit", autosubmit_target: "form" }) do |form| %>
    <% if tag.errors.any? %>
      <div class="text-danger">
        <h2><%= t('forms.errors.header', count: tag.errors.count) %></h2>
        <ul>
          <% tag.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <div>
      <%= form.label :name_en, t('forms.labels.create_tag') + ' (English)', class: "nowrap" %>
      <div class="d-flex flex-row justify-content-start align-items-center gap-3 mb-2">
        <%= form.text_field :name_en, placeholder: t('forms.placeholders.name') + ' (English)', class: "w-25", required: true %>
        <%= form.collection_select :tag_type_id, @tag_types, :id, :translated_name, 
            { prompt: t('forms.labels.select_tag_type'), include_blank: true }, 
            { class: "form-select w-25", required: true } %>
        <%= form.submit t('forms.buttons.create') %>
      </div>
      <%= form.label :name_ru, t('forms.labels.create_tag') + ' (Russian)', class: "nowrap" %>
      <div class="d-flex flex-row justify-content-start align-items-center gap-3">
        <%= form.text_field :name_ru, placeholder: t('forms.placeholders.name') + ' (Russian)', class: "w-25" %>
      </div>
    </div>
  <% end %>
<% end %>
