<div class="d-flex justify-content-between align-items-center mb-3">
  <h1><%= t('families.edit.title') %></h1>
  <%= link_to t('forms.buttons.back_to_families'), families_path, class: "btn btn-secondary" %>
</div>

<%= render "form", family: @family %>

<%= render "tag_assignment" %>
